# Week 2 修复后重新验收计划

状态：预登记后执行。旧运行结果只用于失败归因，不覆盖、不删除。

## 修复范围

- 兼容单行文本 ReAct、JSON ReAct、`snake_case` JSON 字段和全角冒号；
- 对只有参数、没有 Tool 名称的输出继续失败关闭，但返回可操作的格式纠正信息；
- 只对严格 `{"sql": ...}` 和 `{"output": ...}` 等无歧义对象做协议恢复；
- 明确组合题数量词的参数作用域，禁止把一个子任务的 `top_n` 传播给其他 Tool；
- 不按题号、Gold Tool 或期望参数建立路由或补偿逻辑。

## 冻结资产

- 数据集：`insight_agent/evaluation/dataset/tool_selection_test.json`
- SHA-256：`996e11265a0bd9efaa3a4a3bf56267a9b94964f24102e1ce0d93602d67521603`
- 题量：40，划分为 24 dev、12 test、4 challenge；
- 温度：0；每题使用独立会话；
- 原始输出、调用参数、Observation、延迟和失败类型全部保留。

## 执行矩阵

1. DeepSeek Flash：baseline、positive、boundary 三配置各完整运行 3 次；
2. DeepSeek Pro：在同一冻结代码和 boundary 配置上完整运行 1 次；
3. 不因单题或整轮失败补跑，不选择最好一次，不覆盖旧失败轨迹；
4. 重新运行生产 RAG、业务 Tool、集成 HTML 三条烟测；
5. 最终执行完整自动化回归、Ruff、差异和凭据检查。

## 正式门槛

- Tool 选择准确率不低于 90%；
- 三个业务 Tool 各自召回率不低于 80%；
- 参数有效率不低于 95%；
- 多余调用率不高于 5%；
- 非法参数进入数据库为 0；
- 自动化回归与代码检查全部通过。

## 解释边界

这是在已观察旧失败后的修复回归，不是新的独立盲测。重新验收只能证明协议兼容修复在同一冻结集上满足门槛；不能把结果扩大为生产准确率，也不能抹去首次 Pro 14/40 的历史结果。
